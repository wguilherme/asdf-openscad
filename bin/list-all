#!/usr/bin/env bash
set -euo pipefail

curl -fsSL \
  ${GITHUB_API_TOKEN:+-H "Authorization: token $GITHUB_API_TOKEN"} \
  "https://api.github.com/repos/openscad/openscad/releases?per_page=100" \
  | grep '"tag_name"' \
  | sed 's/.*"tag_name": "openscad-\([^"]*\)".*/\1/' \
  | sort -V
